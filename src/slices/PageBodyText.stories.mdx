import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import faker from 'faker'

import * as Storybook from '../components/Storybook'

import PageBodyText from './PageBodyText.tsx'

export const exampleHTML = `
  <h1>Heading 1</h1>
  <p>${faker.lorem.paragraph(1)}</p>
  <p>${faker.lorem.paragraph(1)}</p>
  <h2>Heading 2</h2>
  <p>${faker.lorem.paragraph()}</p>
  <ul>
    <li>${faker.lorem.sentence(3)}</li>
    <li>${faker.lorem.sentence(3)}</li>
    <li>${faker.lorem.sentence(3)}</li>
  </ul>
  <h3>Heading 3</h3>
  <p>${faker.lorem.paragraph()}</p>
  <ol>
    <li>${faker.lorem.sentence(3)}</li>
    <li>${faker.lorem.sentence(3)}</li>
    <li>${faker.lorem.sentence(3)}</li>
  </ol>
  <h4>Heading 4</h4>
  <p>${faker.lorem.paragraph()}</p>
  <p><a href="#">Link ${faker.lorem.words(3)}</a></p>
  <p><span class="large">Large label</span></p>
  <p><span class="small">Small label</span></p>
  <h5>Heading 5</h5>
  <p>${faker.lorem.paragraph()}</p>
  <p>${faker.lorem.paragraph()}</p>
  <h6>Heading 6</h6>
  <p>${faker.lorem.paragraph()}</p>
`

<Meta title="Pages/Text" component={PageBodyText} />

#### Page

# Text

Text block that allows rich formatting, such as headings, bold and italicized
text, and links.

#### Slice fields

<Storybook.SliceFieldsTable
  customTypeId="page"
  sliceZoneId="body"
  sliceId="text"
  fieldset="non-repeat"
/>

### With long text

Usage of this block won't typically contain every heading and formatting
option. This example shows every available option, however.

<Canvas withSource="none">
  <Story name="With long text">
    <PageBodyText
      nextSharesBg={false}
      textHTML={exampleHTML}
    />
  </Story>
</Canvas>
